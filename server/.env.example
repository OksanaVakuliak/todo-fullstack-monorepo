# Port where the Express API will listen.
PORT=4000

# MongoDB connection string used by Mongoose.
# Replace this value with your own local or MongoDB Atlas database URI.
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/todo_app?retryWrites=true&w=majority
