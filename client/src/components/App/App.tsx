export default function App() {
  return (
    <main className="app-shell">
      <section className="app-card">
        <p className="app-eyebrow">Todo List</p>
        <h1>Frontend setup is ready</h1>
        <p>
          Next I will connect the form, task list, and API calls with Axios.
        </p>
      </section>
    </main>
  );
}
